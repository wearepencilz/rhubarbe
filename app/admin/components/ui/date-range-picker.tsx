'use client';

/**
 * DateRangePicker wrapper — re-exports the Untitled UI DateRangePicker from
 * the date-picker sub-directory for convenient top-level imports.
 *
 * Usage:
 *   import { DateRangePicker } from '@/app/admin/components/ui/date-range-picker';
 */
export { DateRangePicker } from './date-picker/date-range-picker';
