{
  "enabled": false,
  "name": "Run CMS Tests (Legacy)",
  "description": "DISABLED — references legacy src/cms/__tests__ path that no longer exists.",
  "version": "1",
  "when": {
    "type": "userTriggered"
  },
  "then": {
    "type": "runCommand",
    "command": "echo 'Legacy test path — update to current test location'"
  }
}
